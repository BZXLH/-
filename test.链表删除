//链表删除操作

link *DelLink(link *head,int n)
{
	link *q= head;
	link *p= head;
	for(int i= 1;i<=n;i++)
	{
		q= p;
		p= p->next;
		if(p==NULL)
			return head;
	}
	q->next= p->next;
	delete p;
	return head;
}
